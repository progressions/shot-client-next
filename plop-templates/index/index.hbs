export { default as View } from "@/components/{{ snakeCase (plural name) }}/table/View"
export { default as Menu } from "@/components/{{ snakeCase (plural name) }}/Menu"
export { default as SpeedDial } from "@/components/{{ snakeCase (plural name) }}/SpeedDial"
export { default as {{pascalCase (plural name)}}Mobile } from "@/components/{{ snakeCase (plural name) }}/{{pascalCase (plural name)}}Mobile"
export { default as TableHeader } from "@/components/{{ snakeCase (plural name) }}/table/TableHeader"
export { default as TableBody } from "@/components/{{ snakeCase (plural name) }}/table/TableBody"
export { default as {{pascalCase (singular name)}}Filter } from "@/components/{{ snakeCase (plural name) }}/{{pascalCase (singular name)}}Filter"
export { default as {{pascalCase (singular name)}}Form } from "@/components/{{ snakeCase (plural name) }}/{{pascalCase (singular name)}}Form"
export { default as Create{{pascalCase (singular name)}}Form } from "@/components/{{ snakeCase (plural name) }}/Create{{pascalCase (singular name)}}Form"
