/**
 * Shared constants for location zone components
 */

/** Default width for location zones (pixels) */
export const DEFAULT_ZONE_WIDTH = 200

/** Default height for location zones (pixels) */
export const DEFAULT_ZONE_HEIGHT = 150

/** Minimum width for location zones (pixels) */
export const MIN_ZONE_WIDTH = 150

/** Minimum height for location zones (pixels) */
export const MIN_ZONE_HEIGHT = 100

/** Spacing between zones in canvas mode (pixels) */
export const ZONE_SPACING = 20

/** Minimum canvas dimensions (pixels) */
export const CANVAS_MIN_WIDTH = 800
export const CANVAS_MIN_HEIGHT = 500

/** Minimum gap between zones for collision detection (pixels) */
export const COLLISION_PADDING = 4

/** Connection line styling */
export const CONNECTION_LINE_COLOR = "#1976d2"
export const CONNECTION_LINE_WIDTH = 2
export const CONNECTION_LINE_HOVER_WIDTH = 4
export const CONNECTION_HIT_WIDTH = 16 // Invisible wider stroke for easier clicking

/** Connection handle (anchor point) styling */
export const CONNECTION_HANDLE_SIZE = 16
export const CONNECTION_HANDLE_COLOR = "#1976d2"
export const CONNECTION_HANDLE_HOVER_COLOR = "#1565c0"

/** Arrowhead dimensions */
export const ARROWHEAD_SIZE = 10
