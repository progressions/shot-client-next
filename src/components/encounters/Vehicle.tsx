import { Box, Typography } from "@mui/material"
import type { ShotDetails } from "@/types"
import { CS } from "@/services"

interface VehicleProps {
  detail: ShotDetails
}

export default function Vehicle({ detail }: VehicleProps) {
  return (
    <>
      hello
    </>
  )
}

